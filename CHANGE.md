# 변경 이력 (CHANGE.md)

> 최근 변경 사항을 날짜별로 정리합니다. (최신이 위)

## 2026-07-11

### 일정 카테고리 — 색상 선택 + 색 입힌 칩
- 카테고리 추가를 `prompt` → **모달**로 변경: 이름 입력 + **원형 색상환(HSV) + 명도 슬라이더**로 임의 색 선택,
  또는 **10색 프리셋**에서 빠른 선택 (색상환·프리셋·명도가 미리보기/hex로 실시간 동기화)
  — 색상환은 외부 라이브러리 없이 캔버스로 직접 구현 (`js/lib/colorpicker.js`)
- 선택한 색을 `site/eventCategories` 의 **`colors` 맵**에 저장 → 배지·D-day 카드 등 전역에 반영
  (format.js `catColor` 가 사용자 지정 색을 최우선 사용, 없으면 기존 기본색/자동배정)
- '일정 만들기'의 카테고리 칩이 **각자의 색(파스텔 배경 + 원색 테두리)**으로 표시되어 색을 한눈에 구분
- '카테고리' 라벨 옆 **연필(수정) 아이콘** → **카테고리 수정 모달**: 등록된 카테고리를 골라
  **이름·색상 재선택**. 이름을 바꾸면 그 카테고리로 등록된 **일정들도 새 이름으로 자동 이동**(연결 유지)
- 카테고리 삭제 시 저장된 색도 함께 정리 (색상 위젯은 추가·수정 모달이 공유)

### 하단 탭 문구 '페이지 관리' → '관리'
- 크루 공간·관리 화면의 하단 탭 라벨을 **'페이지 관리' → '관리'**로 축소 (헤더 문구와 통일)
- 관련 코드(탭 title·주석)와 PROJECT.md 설명도 함께 정리 (관리 페이지의 정식 이름은 그대로 유지)

### 멤버 탭 승인 대기 알림 배지
- 운영진이면서 **승인 대기자가 있을 때**, 하단 '멤버' 탭 아이콘 우측 상단에 **빨간 배지**로 대기 인원수 표시
- 대기 0명이거나 운영진이 아니면 숨김, 100명 이상이면 '99+'
- 멤버 실시간 구독으로 갱신되어, 다른 탭을 보고 있어도 신규 신청이 오면 바로 배지가 뜸

### PWA 자동 업데이트 알림
- 앱을 켤 때·다시 화면에 돌아올 때마다 백그라운드로 `sw.js` 변경을 확인
- 새 버전이 감지되면 화면 하단에 **"새 버전이 출시되었습니다!"** 토스트 표시(업데이트·닫기 버튼)
- **업데이트** 누르면 대기 중인 새 서비스 워커를 즉시 활성화(skipWaiting) → 자동 새로고침으로 최신 반영
- 첫 방문의 `clients.claim()` 으로 인한 불필요한 새로고침은 막음(사용자가 업데이트를 누른 경우에만 새로고침)
- 배포 방법: `sw.js` 의 **VERSION 을 올린 뒤 배포**하면 알림이 뜸 (안 올려도 온라인이면 최신 파일은 네트워크 우선으로 자동 반영되지만, '켜둔 앱'에 알림을 주려면 올릴 것)
- 확인: 변경 감지→토스트→대기→업데이트 클릭→새로고침 전체 사이클 8건 통과

### PWA 전환 — 홈 화면 설치 + 오프라인 앱 셸
- **웹 앱 매니페스트**(`manifest.webmanifest`): 이름·아이콘·테마색(#241c14) 등, 설치 시 **크루 공간(app.html)**으로 시작, 세로 고정 standalone
- **아이콘 5종** 생성(`icons/`): 일반(192·512)·마스커블(192·512, 크림 배경 안전영역)·apple-touch(180) — `img/logo.png`에서 자동 생성
- **서비스 워커**(`sw.js`): 앱 셸(HTML·CSS·JS·이미지) 캐시, "네트워크 우선 → 실패 시 캐시" 전략으로 온라인이면 항상 최신·오프라인이면 캐시로 동작 (Firestore 데이터는 네트워크). 외부(Firebase·폰트 CDN)는 건드리지 않음
- **설치 안내 배너**: 크루 공간 홈 상단에 '홈 화면에 추가' 배너(설치 가능할 때만) — 설치/닫기 버튼, 이미 설치된 상태나 이번 세션에 닫으면 숨김
- 세 페이지 head에 매니페스트·테마색·apple 메타 추가, 각 페이지에서 서비스 워커 등록(`js/lib/pwa.js`)
- 검증: 매니페스트·아이콘 5종·서비스 워커 루트 등록·배너 노출 등 14건 통과

### 일정 카드 레이아웃 개편 (참석자 관리 모달화)
- **왼쪽 열은 날짜 배지만** 단독 배치 (참석 버튼을 아래에서 뺌)
- 카드 하단 한 줄에 **왼쪽=참석자 요약 / 오른쪽=참석 여부 '참석' 버튼**을 나란히 배치
  (참석자가 없어도 참석 버튼은 항상 우측 정렬)
- 운영진 **'참석자 관리'를 인라인 펼치기 → 모달 팝업**으로 변경
  - 참석자 목록이 보이는 '참석 N …' 요약을 누르면 관리 모달이 열림
  - 모달에서 크루원 이름을 눌러 참석 여부를 바로 저장 (지난 일정은 열 때 출석을 불러옴)
  - 모달·출석 현황 패널이 같은 저장 로직을 공유, 열려 있으면 실시간으로 목록 갱신
- 크루원(비운영진)은 이전처럼 참석자 이름만 표시(관리 버튼 없음)

### 로고 이미지 적용 + 첫 방문 스플래시
- 헤더(C.R.C 옆)·푸터·로딩 스피너의 당근 마크가 **`img/logo.png` 이미지**를 우선 사용
  — 파일이 없으면 기존 SVG 당근으로 자동 대체 (`onerror` 폴백)라 이미지를 넣기 전에도 정상 동작
  — **`public/img/logo.png` 에 크루 로고 파일을 넣으면 즉시 적용** (투명 배경 정사각 PNG 권장)
- 소개 페이지 **첫 방문(캐시 없음·시크릿 모드) 스플래시**: 기본값 문구가 잠깐 보이는 대신
  로고 이미지 + **C.R.C** 글자(어두운 배경 + 하단 연석 라인)를 먼저 보여주고,
  Firestore 콘텐츠 준비가 끝나면 부드럽게 걷어냄 (안전장치: 최대 6초 후 자동 해제)
  — 캐시가 있는 재방문은 스플래시 없이 저장값으로 바로 시작
- 검증: 이미지 없음(폴백)·이미지 있음 두 모드 각 10건 전부 통과

### 소개 페이지 기본값 깜빡임 해결 (저장 콘텐츠 로컬 캐시)
- 접속 시 site-data.js 기본값(예: 크루 멤버 "40+")이 잠깐 보였다가
  Firestore 저장값("10+")으로 바뀌던 문제를 근본 해결
- 마지막으로 불러온 저장 콘텐츠를 **localStorage 에 캐시** → 재방문 첫 화면부터
  기본값 대신 저장값으로 그림 (문구·통계·핵심 가치·정기런·가입 안내·문의 전부)
- 운영진이 **페이지 관리에서 저장하는 순간 캐시도 함께 갱신** → 저장 직후
  같은 기기에서 소개 페이지를 열어도 이전 값 깜빡임 없이 새 내용이 바로 보임
- 캐시가 없거나(첫 방문) 손상된 경우·localStorage 불가 환경은 기존처럼 기본값→저장값 순서로 안전 동작
- 검증: 재방문 첫 페인트에 기본값 미노출·저장값 일치, 손상 캐시 안전 렌더 등 5건 통과

### 페이지 관리에도 하단 탭 유지
- 관리 화면(admin.html) 하단에 크루 공간과 동일한 5탭 표시 ('페이지 관리'가 활성)
- 홈/일정·출첵/소식/멤버를 누르면 `app.html#tab=…` 으로 이동해 **해당 탭이 바로 열림**
  (크루 공간이 해시를 읽어 탭 전환 후 주소를 정리)
- 하단 탭 CSS를 app.css → components.css 로 이동 (두 페이지 공용),
  이동 과정에서 모바일 미디어쿼리가 하단 여백을 덮어쓰던 문제를 특이도 조정으로 해결

### 아이콘 전면 교체 — 이모지·알약 탈피 (세 페이지 공통)
- **SVG 라인 아이콘 도입** (`js/lib/icons.js` 신설): 2px 스트로크·각진 끝처리의 자체 제작 아이콘 17종
  — 하단 탭(홈·달력·확성기·멤버·슬라이더), 참석 깃발, 수정 연필, 시간·장소, 고정핀, 왕관, 열쇠, 자물쇠 등
- **당근 로고 SVG 제작**: 🥕 이모지 → 두 색(주황 몸통 + 초록 잎) 지오메트릭 당근 마크
  — 헤더·푸터 로고, 로그인 화면, 로딩 스피너, 출석 스탬프, 갤러리 자리표시에 통일 적용
  — 파비콘도 `favicon.svg` 파일로 교체 (기기마다 다르게 보이던 이모지 문제 해결)
- **메달(🥇🥈🥉) → 순위 칩**: 이달의 기록 1·2·3위를 배번호처럼 기울인 금·은·동 각진 칩으로
- **알약(999px) 모양 24곳 전부 각지게**: 탭·버튼 8px, 배지·칩 4px (레이스 데이 컨셉에 맞춤)
- **문장 꼬리 이모지 정리**: "반가워요! 🥕", "저장했어요 ✅" 등 안내문·알림의 장식 이모지 제거
- 핵심 가치 카드 아이콘(🥕🌱🧡 등)은 운영진이 관리 페이지에서 입력하는 콘텐츠라 유지
- 검증: 세 페이지 헤드리스 테스트 18건 통과 (UI 이모지 잔존 0건)

### 디자인 리뉴얼 — '레이스 데이' (스포티 + 트랙 모티프, 세 페이지 공통)
- 주황 브랜드 색은 유지하면서 육상 트랙 요소를 곳곳에:
  - **연석(kerb) 라인**: 헤더 아래 · 하단 탭 위 · 소개 히어로 하단에 주황/흰 반복 띠
  - **스피드 마크(//)**: 모든 섹션 제목 앞에 기울어진 주황 줄무늬
  - **배번호(bib) 날짜 박스**: 일정 카드 날짜를 마라톤 배번호 스타일로
    (주황 띠 + 흰 판 + 굵은 검정 숫자, 지난 일정은 회색 띠)
  - **스피드 기울임**: D-day 숫자·크루 현황 숫자를 살짝 기울여 속도감
  - D-day 카드에 비스듬한 트랙 레인 줄무늬 오버레이
- 카드 모서리를 각지게(18→12px), primary 버튼은 유니폼 투톤 슬래시로
- 세 페이지 회귀 테스트 통과 (기능 변화 없음)
- 운영진 서브탭에 **[내보내기]** 추가 (승인 대기 · 거절 · 내보내기) — 3개가 화면 폭과 무관하게 **한 줄** 유지
- '내보내기'가 이제 멤버 문서를 지우지 않고 **기록으로 보존** (`role: removed` + 내보낸 날짜)
  — 내보내기 탭에서 **↩ 멤버로 복구** 또는 **기록 삭제**(삭제하면 재로그인 시 승인 대기로 새로 등록)
- 내보내진 사람이 로그인하면 전용 안내 화면 표시 ("크루에서 나가게 되었어요")
- 멤버 목록 배지 정리: '멤버' 배지 삭제, 운영진은 **👑 아이콘만** 표시 (모든 크루원에게 보임)
- 보안 규칙 변경 없음 (역할 변경은 기존 규칙으로 허용됨)

### 홈 바로가기 · 일정 카드 레이아웃 · 관리 헤더 문구
- 홈 바로가기 정리 — 어디를 눌러도 맞는 화면으로:
  '전체 보기'·D-day 카드·**다가오는 일정의 각 행** → 일정·출첵의 '일정' 서브탭 /
  **고정 공지 배너** → 소식의 '공지' 필터 / **투표 카드·'더 보기'** → 소식의 '투표' 필터
- 일정 카드: 날짜 박스를 조금 줄이고, **참석 버튼을 날짜 아래**에 배치 (날짜 > 버튼 크기)
- 페이지 관리 상단 헤더 문구 '페이지 관리' → **'관리'**

## 2026-07-10

### 미사용 코드 정리 (동작 변화 없음)
- 이전 개편들의 잔재 CSS 규칙 삭제(약 160줄): 옛 멤버 카드, 옛 참석 버튼, 기록 표 머리·합계,
  소개 페이지 가입 폼, 구글 로그인 버튼, 공지 핀 배지, 관리자 상단 탭 등
- 미사용 JS 함수 삭제: `parseTimeStr`·`fmtDur`·`fmtPace` (시간 입력·랭킹 삭제로 호출처 소멸)
- 내부에서만 쓰는 값의 export 정리, 낡은 주석 경로 수정
- 검증: 미사용 클래스/export 자동 감사 0건 + 세 페이지 헤드리스 회귀 테스트 21건 통과

### 페이지 관리 개편 — 미리보기 보면서 한 화면에서 수정
- ⚙️ 페이지 관리 진입을 헤더 → **크루 공간 하단 탭 맨 오른쪽**으로 이동 (운영진에게만 표시)
- 탭 구분 없이 **한 화면**에 소개 페이지 순서 그대로 섹션 나열:
  기본 → 크루 현황 → 핵심 가치 → 정기런 → 공식 기록 → 갤러리 → 가입 안내·문의
- 문구류 섹션은 위에 **소개 페이지에 보이는 실제 모습 그대로 미리보기** 표시
  (소개 페이지 스타일을 그대로 재사용) — 아래 폼에서 고치면 **즉시 미리보기에 반영**, '저장'해야 실제 적용
- 항목을 모두 지우면 미리보기에 "소개 페이지에서 숨겨져요" 안내

### 일정·출첵 탭 개편 — '이달의 기록'으로 통합
- 서브탭을 **3개**로 정리하고 화면 폭과 무관하게 **한 줄**에 표시: 📅 일정 · 🥕 출석 현황 · 🏆 이달의 기록
- **'기록 보기' + '이달의 랭킹' → '이달의 기록'으로 통합** (랭킹 탭 삭제)
  - **월별 표**로 변경: `이름 | 출석 | 목표(km) | 누적(km)` — ‹ › 로 월 이동 (출석 현황과 달 공유)
  - '각오' 삭제, '출석' 추가(정기런 기준), '현재(km)' → '누적(km)'
  - 🥇🥈🥉 메달: **1순위 출석, 2순위 누적 km** (출석·누적이 모두 0이면 메달 없음)
  - '오늘의 마일리지' 저장은 이제 **그 달의 누적**에 더해짐 — 데이터가 `mileage/{uid}.months` 맵(월별)으로 바뀜 (규칙 변경 없음)
- 일정 카드: 참석·✏️·✕ 버튼을 모두 **같은 크기로 작게 우측 상단 구석**에 배치 (제목은 그 아래에서 전체 폭 사용)
- 홈: D-day 카드를 **모두 같은 크기**로 통일 (첫 카드만 크게 강조하던 것 제거)
- 출석 현황: 날짜 칩의 🥕 를 `:` 구분자로 (`7/4(토) : 5`), 요약 칩을 **정기런 · 총 참석수 · 평균 출석률** 3개로 정리('총 거리' 삭제)하고 화면 폭을 균등하게 나눠 배치, 표 아래 안내 문구 간소화

### 폴더 구조 개편 (동작 변화 없음)
- 기준: **공통(두 화면 이상) ↔ 화면 전용(pages/)** — 빌드 도구 없이 브라우저 ES 모듈로 동작
- CSS: 버전별 파일(style/v2/v3)을 역할별로 재배치
  → `base.css`(토큰·리셋·버튼·헤더) + `components.css`(공용 컴포넌트) + `pages/{public,app,admin}.css`
- JS 공통 모듈 `js/lib/` 신설: `firebase.js`(초기화) · `account.js`(계정 변환+힌트 암호화, app/admin 중복 제거)
  · `format.js`(이스케이프·날짜·페이스·카테고리 색) · `ui.js`(모달·폼 메시지)
- `public.js`/`admin.js`/`site-data.js` → `js/pages/public/`, `js/pages/admin/` 으로 이동
- 1,900줄 `app.js` 를 화면별 모듈로 분리 → `js/pages/app/`
  (`main` 진입점 · `init` 라우팅/구독/탭 · `state` 공유 상태 · `auth` · `home` · `events` · `news` · `members`)
- 검증: CSS 규칙 보존 자동 비교(누락 0), 페이지 3종 로드·기능 테스트 47건 전부 통과

## 2026-07-09

### '기록 보기'(마일리지 보드)로 개편 + 출첵 표 개선
- 서브탭 '일정별 기록' → **'기록 보기'** — 일정 선택 칩 삭제, 일정과 무관한 **마일리지 보드**로 변경
- **오늘의 마일리지**: 날짜가 표시되는 입력 박스에 거리(km)만 입력 → 저장하면 **현재 마일리지에 누적**
  (시간 입력·페이스 계산·출첵 연동 삭제, 문구 '저장하면 마일리지가 누적되어요!')
- 기록 보기 표: `이름 | 각오 | 목표 마일리지(km) | 현재 마일리지(km)` — 현재 마일리지 많은 순
- 본인은 ✏️로 자기 각오·목표·현재 마일리지 수정, 운영진은 모든 크루원 수정 가능 (수정 모달)
- 데이터: 새 `mileage/{uid}` 컬렉션 → **보안 규칙 재배포 필요**
- 일정 카드 버튼: ✏️/✕ 배경을 카드와 같은 흰색으로, ✕는 빨간색
- 출첵 표: 이름 열 가운데 정렬, 날짜가 5개를 넘으면 열이 좁아지지 않고 **하단 좌우 스크롤**로 확인

### 출첵 관리 로직 개편 + 일정 버튼 아이콘화
- 일정 카드의 수정/삭제 버튼을 **✏️ / ✕ 아이콘**으로 변경
- 기본 카테고리를 **대회 · 정기런 · 모임**으로 변경 (이미 ＋/－로 저장한 목록이 있으면 그 목록이 우선)
- 참석자 이름은 **3명 초과 시 '외 x명'** 으로 축약 표시
- **출석 현황은 '정기런' 일정만 출첵으로 집계** (매트릭스·요약 칩 모두)
- 출석 현황 상단(월 표시 위)에 그 달의 정기런을 **한 줄 날짜 칩**(`7/4(토) 🥕5`)으로 표시
  — 칩을 누르면 그 날짜의 참석자가 아래 패널 하나에 표시 (다시 누르면 닫힘, 공간 절약)
- 운영진 참석자 수정: **일정 카드**('👥 참석자 관리' 펼치기)와 **출석 현황 날짜 칩 패널**에서
  크루원 체크박스로 모든 카테고리 일정의 참석자를 바로 수정 (기록 있는 참석자 해제 시 확인 창)

### 일정 카드·멤버 목록 UI 개선
- '다가오는 일정'에서 **불참 버튼 삭제**, **참석 버튼을 카드 우측 상단**으로 이동
  — 수정·삭제 버튼과 같은 모양, 운영진에겐 참석 → 수정 → 삭제 순서. 참석 중이면 초록색, 다시 누르면 취소
- 멤버 목록을 **한 줄 행**으로 변경, 아바타 삭제
  — 멤버에게는 `이름 | 합류날짜`, 운영진에게는 `이름 | 합류날짜 | role | 운영진 지정/해제 | 내보내기`

### 크루 공간 탭 개편 — 하단 고정 4탭
- 상단 탭(공지/일정·출첵/투표/멤버)을 **하단 고정 탭**으로 이동: **🏠 홈 · 🥕 일정·출첵 · 📢 소식 · 👥 멤버**
- **공지 + 투표를 '소식' 탭으로 통합** — 전체/공지/투표 필터로 골라 보기
- **홈(대시보드) 신설**:
  - 카테고리별 가장 가까운 일정 **D-day 카드** (최대 3장, 카테고리 색상)
  - **고정 공지 배너** (누르면 소식 탭으로 이동)
  - **다가오는 일정** 오늘~2주 리스트 (토요일 파랑·일요일 빨강)
  - **진행 중인 투표** 미리보기

### 일정·출첵 탭 기능 추가 (서브탭 4개)
- **📅 일정** — 기존 일정 목록/만들기 (카테고리 필터 포함)
- **🥕 출석 현황** — 월별 출석 매트릭스 (이름 열 고정, 좌우 스크롤, 🥕=참석, 출석 합계),
  요약 칩(일정 수·참석 연인원·총 거리·평균 출석률), ‹ › 로 월 이동
- **⏱️ 일정별 기록** — 일정을 골라 **내 거리·시간 셀프 입력** → 페이스 자동 계산,
  저장하면 출석 🥕 도 함께 인정. 참석자 기록 테이블(거리순)과 합계/평균 페이스/입력 현황 표시.
  운영진은 ✏️ 로 다른 크루원 기록 대신 입력/수정 가능
- **🏆 이달의 랭킹** — 월별 총 거리 기준 랭킹 (🥇🥈🥉, 출석 횟수·평균 페이스 표시)
- 기록은 새 컬렉션 없이 기존 출석 문서(`events/{id}/attendance/{uid}`)에 `dist(km)`·`sec(초)` 필드로 저장
- 참석 취소 시 입력한 기록이 있으면 확인 후 삭제, 참석/불참 변경 시 기록은 유지(merge)
- **보안 규칙 변경**: 운영진이 출석/기록 문서를 대신 작성·수정할 수 있도록 허용
  → `firebase deploy --only firestore:rules` **재배포 필요**

### 일정 카테고리 (커밋 8231919)
- 일정 만들기/수정에 **카테고리 선택** 추가 (기본: 대회·러닝·이벤트·모임)
- 폼에서 ＋/－ 버튼으로 카테고리 목록 관리 (`site/eventCategories` 에 저장, 규칙 변경 없음)
- 일정 목록에 카테고리 **색상 배지** + 상단 **필터 칩**
- 날짜·시간 입력을 모바일에서도 한 줄로 배치
- 공지 펼침 화면의 작성자·고정·수정·삭제 버튼을 본문 위(상단)로 이동

## 2026-07-08

### 가입 신청·계정 체계 개편 (커밋 193d323)
- 가입 신청을 크루 공간으로 통합: '회원가입' → **가입신청** 탭, 메인의 '크루 가입하기'가 바로 가입신청 화면으로 연결
- 신청 정보 확장: 이름·성별(한 줄), 연락처(하이픈 자동), 사는 곳, 연령대, 대회 경력, 하고 싶은 말 — 성별/연령대/경력은 라디오 버튼
- **계정 아이디화**: 이메일 형식 대신 순수 아이디 사용 (영문·숫자·`.` `_` `-`), 대소문자 구분,
  내부적으로만 `@crc.ulsan` 가짜 도메인으로 변환 (대문자는 `+소문자` 인코딩으로 보존)
- 중복 계정이면 "이미 사용 중인 계정이에요" 안내 후 재입력
- **비밀번호 찾기 개편**: 가입 시 힌트 질문/답변 등록 (답변 대소문자 구분) →
  '비밀번호를 잊었나요'에서 계정+질문+답변이 맞으면 새 비밀번호 즉시 설정.
  비밀번호는 답변으로 암호화되어 `pwhints` 컬렉션에 저장 (PBKDF2 + AES-GCM)
- 내 정보 수정에서 힌트 등록/변경 가능, 비밀번호 변경 시 힌트 자동 갱신

### 소개 페이지 섹션 자동 관리 (커밋 193d323)
- '크루 가입 신청' 섹션(신청서 폼) 삭제 — 신청은 크루 공간에서만
- 내용 없는 섹션 자동 숨김: 크루 현황 숫자·핵심 가치·정기런 일정·공식 기록·갤러리·가입 안내·가입 문의
- **가입 안내(JOIN US)** 와 **가입 문의** 내용을 페이지 관리(운영진)에서 편집 — 저장하면 표시, 비우면 숨김

### 공지 목록 UI 개선 (커밋 193d323)
- 공지를 `고정핀 | 제목 | 날짜` 한 줄 목록으로, 누르면 펼쳐서 내용 확인
- 고정 글에만 📌 표시, 버튼 순서 고정(해제) → 수정 → 삭제
- 크루 공간 헤더에 '크루공간' 문구 복원, 로그인 후 이름 밑줄 제거
- 프로젝트 문서를 PROJECT.md 로 정리

### 글 수정 기능 (커밋 a185d97)
- 공지·일정·투표에 **수정 버튼** 추가 (수정 모달, '(수정됨)' 표시)
