# 🥕 당근러닝크루 (C.R.C) 웹사이트

울산 당근러닝크루 웹사이트. **소개 페이지 + 크루원 전용 공간 + 운영진 페이지 관리**로 구성됩니다.
Firebase(Hosting + Auth + Firestore) 기반의 정적 사이트이며, **무료 플랜(Spark)** 으로 운영합니다.
크루원 대부분이 모바일로 접속하므로 **모바일 화면을 우선**으로 만들었습니다 (데스크탑도 동일 기능).

## 한눈에 보기

| 페이지 | 대상 | 하는 일 |
|---|---|---|
| `index.html` 소개 페이지 | 누구나 | 크루 소개 · 정기런 일정 · 공식 기록 · 갤러리 · 가입 안내/문의 |
| `app.html` 크루 공간 | 크루원 (로그인) | 가입 신청, 공지, 일정·출석체크, 투표, 멤버 관리(운영진), 내 정보 수정 |
| `admin.html` 페이지 관리 | 운영진만 | 소개 페이지의 문구·공식 기록·갤러리를 직접 수정 (저장 즉시 반영) |

**전체 흐름**: 방문자가 소개 페이지에서 크루를 알아보고 → "크루 가입하기"로 가입 신청서 작성 →
운영진이 멤버 탭에서 신청 정보를 보고 승인/거절 → 크루원이 되면 공지·일정·출첵·투표 이용.

## 페이지별 기능

### 1. 소개 페이지 (index.html)

위에서부터: 히어로(슬로건 + 크루 현황 숫자) → 소개/핵심 가치 → 정기런 일정 → 공식 기록 →
갤러리 → 가입 안내(JOIN US) → 가입 문의.

**내용이 없는 섹션은 자동으로 숨겨집니다** (화면 + 상단/하단 메뉴 링크까지):

| 섹션 | 표시 조건 |
|---|---|
| 크루 현황 숫자 · 핵심 가치 · 정기런 일정 | 기본값(site-data.js) 또는 저장 내용이 있으면 표시. 페이지 관리에서 항목을 모두 지우고 저장하면 숨김 |
| 공식 기록 · 갤러리 · 가입 안내 · 가입 문의 | **기본값 없음** — 페이지 관리에서 저장한 내용이 있을 때만 표시 |

- 공식 기록: 연도 탭 → 대회 카드 → 종목(풀코스/하프/10km/5km) 탭, 기록순 정렬 + 순위 표시
- 갤러리: 앨범(커버·이름·장수) 그리드 → 앨범 클릭 시 전체화면 사진 뷰어
- 히어로의 "크루 가입하기" 버튼 → 크루 공간 가입신청 화면으로 바로 이동 (`app.html#signup`)

### 2. 크루 공간 (app.html)

**로그인/가입신청**: [로그인] [가입신청] 탭. 역할(role)에 따라 승인 대기/거절 안내 화면 또는 본화면으로 이동.

로그인하면 **화면 하단 고정 탭 4개**로 이동합니다.

| 하단 탭 | 기능 |
|---|---|
| 🏠 홈 | 대시보드 — 카테고리별 가장 가까운 일정 **D-day 카드**(최대 4장, 같은 크기), **고정 공지 배너**, 오늘~2주 **다가오는 일정**, **진행 중인 투표** 미리보기. 누르면 해당 화면으로 이동: 일정 관련 → '일정' 서브탭, 공지 배너 → 소식 '공지' 필터, 투표 → 소식 '투표' 필터 |
| 🥕 일정·출첵 | 서브탭 3개 (아래 표) |
| 📢 소식 | **공지 + 투표 통합.** 전체/공지/투표 필터. 공지: `고정(📌) \| 제목 \| 날짜` 목록, 펼쳐서 확인, 운영진 작성·고정·수정·삭제. 투표: 선택지 투표(표 변경 가능)·실시간 집계, 운영진 시작·수정·마감·삭제 |
| 👥 멤버 | 크루 멤버 **한 줄 목록** — `이름 \| 합류날짜` + 운영진에게만 👑 배지(멤버는 배지 없음), 운영진 화면엔 운영진 지정/해제·내보내기 버튼 추가. 운영진 전용: **[승인 대기] [거절] [내보내기] 서브탭** — 신청 정보(성별·연령대·경력·연락처·사는 곳·하고 싶은 말) 확인 후 ✓ 승인 / 거절, 거절·내보내기 기록 보존(`role: rejected/removed`) → 복구 또는 기록 삭제 |

**일정·출첵 서브탭**

| 서브탭 | 기능 |
|---|---|
| 📅 일정 | 일정 목록 + 카테고리 배지/필터. **날짜 박스 아래 참석 버튼**으로 출석체크(참석 중이면 초록색, 다시 누르면 취소), 참석자 명단(3명 초과 시 '외 x명'). 운영진: 만들기(카테고리 ＋/－ 관리), 카드 우측 상단 구석의 작은 ✏️ 수정·✕ 삭제 아이콘, **'👥 참석자 관리' 펼치기 → 체크박스로 모든 카테고리 일정의 참석자 수정** |
| 🥕 출석 현황 | **'정기런' 일정만 출첵으로 집계.** 상단에 그 달 정기런 **날짜 칩**(`7/4(토) : 5`) 한 줄 — 칩을 누르면 그 날짜의 참석자 패널이 열림(운영진은 체크박스로 수정, 다시 누르면 닫힘) + **월별 출석 매트릭스** (이름 열 고정, 좌우 스크롤, 🥕=참석, 합계), 요약 칩(정기런·총 참석수·평균 출석률, 균등 폭), ‹ › 월 이동 |
| 🏆 이달의 기록 | **월별 마일리지 보드 + 메달 랭킹** — '오늘의 마일리지'에 거리(km)를 입력하면 그 달의 누적에 더해짐. 표: `이름 \| 출석 \| 목표(km) \| 누적(km)`, 🥇🥈🥉는 **출석(정기런) 많은 순 → 누적 km 많은 순**. 본인은 ✏️로 자기 목표·누적 수정, 운영진은 전원 수정 가능, ‹ › 월 이동 (출석 현황과 달 공유) |

**내 정보 수정**: 상단의 본인 이름 클릭 → 이름 변경 / 비밀번호 변경(현재 비밀번호 확인) /
비밀번호 힌트 등록·변경. 하단 탭 맨 오른쪽 ⚙️(운영진만 표시) → 페이지 관리로 이동.

### 3. 페이지 관리 (admin.html) — 운영진 전용

크루 공간 하단 탭 맨 오른쪽의 ⚙️ 또는 `/admin.html` 직접 접속. `role: admin` 계정만 이용 가능.
저장 내용은 Firestore 에 저장되어 소개 페이지에 **즉시 반영**됩니다 (재배포 불필요).

**한 화면**에 소개 페이지에 보이는 순서 그대로 섹션을 나열합니다. 문구류 섹션은 위에
**소개 페이지와 똑같은 미리보기**(public.css 재사용)가 있어 아래 폼에서 고치는 즉시
미리보기에 반영 → '저장'해야 실제 적용. 빈 항목이면 미리보기에 "숨겨져요" 안내가 표시됩니다.

| 섹션 (표시 순서) | 관리 내용 |
|---|---|
| 🏠 기본 | 크루 이름·메인/서브 슬로건·소개 문단·인스타 아이디 (히어로+ABOUT 미리보기) |
| 📊 크루 현황 | 히어로 아래 통계 숫자 목록 |
| 🧡 핵심 가치 | 소개 섹션의 가치 카드 (아이콘·제목·설명) |
| 🏃 정기런 | 정기런 일정 카드 (요일·시간·집결·코스·비고) |
| 🏅 공식 기록 | 대회 등록(대회명 + 연월) → 대회마다 종목별 기록(이름·종목·기록) 추가/삭제 |
| 📸 갤러리 | 앨범 만들기 → 사진 업로드(브라우저에서 자동 압축)·삭제 |
| 🤝 가입 안내 · 문의 | JOIN US 절차 목록(번호 자동) + 가입 문의 문구 |

- 사진은 Firebase Storage 없이(무료 플랜 유지) 압축 후 **Firestore 에 base64 로 저장**됩니다.
  한 장당 약 0.3~0.8MB → 앨범당 수십 장, 전체 약 1,500~3,000장까지 무료 범위.

## 계정 · 로그인

- **계정 + 비밀번호** 방식만 사용 (구글·카카오 로그인, 이메일 없음).
- **계정은 아이디 형식**: 영문, 숫자, 점(.), 밑줄(_), 하이픈(-). **대소문자를 구분**합니다 (`HongGil` ≠ `honggil`).
  > 내부 동작: Firebase 인증이 이메일 형식을 요구하고 대문자를 소문자로 바꿔 저장하기 때문에,
  > 대문자는 `+소문자` 로 인코딩하고 뒤에 내부용 도메인을 붙여 저장합니다
  > (`HongGil` → `+hong+gil@crc.ulsan`). 화면에는 항상 원래 아이디만 보입니다.
  > 모바일 키보드의 자동 대문자(첫 글자)는 계정·힌트 답변 입력 칸에서 꺼두었습니다.
  > 예전에 이메일 형식으로 가입한 계정은 이메일 전체를 그대로 입력하면 로그인됩니다.
- **가입 신청 입력 항목**: 계정 · 비밀번호 · 비밀번호 힌트(질문+답변) · 이름 · 성별(남/여) ·
  연락처(하이픈 자동) · 사는 곳(지역/구) · 연령대(10대~50대 이상) · 대회 경력(입문~상급 풀코스) ·
  하고 싶은 말(선택). 이미 사용 중인 계정이면 안내 후 다시 입력받습니다.
- **비밀번호를 잊은 경우**: 로그인 화면 → "비밀번호를 잊었나요?" → 계정·질문·답변 입력 →
  힌트와 일치하면 새 비밀번호 설정 모달이 열리고 즉시 적용 (메일 불필요).
  **답변은 대소문자 구분**, 공백만 무시.
  > 동작 원리: 비밀번호를 "답변"으로 암호화(PBKDF2 31만회 + AES-GCM)해 `pwhints` 컬렉션에 보관 —
  > 답변이 맞을 때만 복호화되어 본인 확인이 됩니다. 답변이 쉬우면(예: 한 자리 숫자) 그만큼
  > 뚫리기 쉬우니 **답변은 남이 추측하기 어렵게** 정하도록 크루원에게 안내하세요.
  > 비밀번호를 변경하면 힌트도 자동 갱신됩니다. 힌트 없이 가입한 기존 계정은
  > 내 정보 수정에서 힌트를 등록해야 비밀번호 찾기를 쓸 수 있습니다.

## 역할(role) 체계

`members` 컬렉션 문서의 `role` 필드로 권한이 정해집니다. 아래 값(소문자)만 인식됩니다.

| role | 의미 | 권한 |
|---|---|---|
| `pending` | 승인 대기 | 가입 신청 직후 자동 부여. "승인 대기 중" 화면만 표시 |
| `member` | 일반 크루원 | 공지·일정·투표 열람, 출석체크, 투표 참여, 본인 이름·비밀번호 변경 |
| `admin` | 운영진 👑 | member 권한 전부 + 공지/일정/투표 작성·수정·삭제, 멤버 승인·거절·내보내기, 운영진 지정, 페이지 관리 접근 |
| `rejected` | 가입 거절 | "가입이 승인되지 않았어요" 화면만 표시. 거절 히스토리용 |

**흐름**: 가입 신청 → `pending` → 운영진이 멤버 탭에서 **✓ 승인** → `member` (거절 시 `rejected`,
나중에 승인으로 변경 가능) → 멤버 카드의 "운영진 지정" → `admin`

> Firebase 콘솔에서 role을 직접 수정하는 것은 **첫 운영진 1명을 지정할 때뿐**입니다.
> `Member`, `크루원` 처럼 다른 값을 넣으면 보안 규칙이 인식하지 못해 아무것도 볼 수 없게 됩니다.

## 데이터 구조 (Firestore)

| 컬렉션 | 내용 | 읽기 | 쓰기 |
|---|---|---|---|
| `members/{uid}` | 이름, 계정, role, 가입 신청 정보(성별·연락처·사는 곳·연령대·경력·하고 싶은 말) | 본인 + 크루원 | 생성: 본인(pending만) / 이름: 본인 / 그 외: 운영진 |
| `notices/{id}` | 공지 (제목·내용·고정·작성자·수정일) | 크루원 | 운영진 |
| `events/{id}` | 일정 (이름·카테고리·날짜·시간·장소·메모) | 크루원 | 운영진 |
| `events/{id}/attendance/{uid}` | 출석 (참석/불참) + 러닝 기록 (`dist` km·`sec` 초) | 크루원 | 본인 + 운영진(대신 입력) |
| `polls/{id}` | 투표 (질문·선택지·마감) | 크루원 | 운영진 |
| `polls/{id}/votes/{uid}` | 표 (선택지 번호) | 크루원 | 본인 |
| `site/content` | 소개 페이지 내용 (문구·현황·가치·정기런·가입 안내·가입 문의) | 누구나 | 운영진 |
| `site/eventCategories` | 일정 카테고리 목록 (기본: 대회·정기런·모임, 폼에서 ＋/－로 관리) | 누구나 | 운영진 |
| `mileage/{uid}` | 마일리지 (목표 km + `months` 맵에 월별 누적 km) | 크루원 | 본인 + 운영진 |
| `records/{raceId}` | 공식 기록 (대회명·연월·기록 목록) | 누구나 | 운영진 |
| `gallery/{albumId}` (+`photos`) | 앨범 (이름·커버·장수) + 사진(base64) | 누구나 | 운영진 |
| `pwhints/{계정주소}` | 비밀번호 힌트 (질문 + 암호화된 비밀번호/답변) | 단건 조회 누구나 | 본인 uid 로만 |

보안은 전부 `firestore.rules` 가 **서버 차원에서** 지킵니다 — 크루원이 아니면 공지/일정/투표를 읽을 수 없습니다.

## 폴더 구조

기준은 **공통이냐 화면 전용이냐**입니다 — 두 화면 이상에서 쓰면 `css/base·components`,
`js/lib`, 한 화면에서만 쓰면 `css/pages`, `js/pages` 아래에 둡니다.

```
public/                     ← Firebase Hosting 으로 배포되는 폴더
├─ index.html               공개 소개 페이지
├─ app.html                 크루 공간 (하단 4탭: 홈/일정·출첵/소식/멤버)
├─ admin.html               페이지 관리 (운영진 전용)
│
├─ css/
│  ├─ base.css              🎨 디자인 토큰(색·폰트·radius)·리셋·버튼·헤더 (전 페이지)
│  ├─ components.css        🧩 공용 컴포넌트: 폼·로그인 카드·탭·작성 폼·카드·모달·btn-mini
│  └─ pages/
│     ├─ public.css         소개 페이지 전용 (히어로·섹션·기록·갤러리·푸터)
│     ├─ app.css            크루 공간 전용 (일정·출첵·소식·멤버·하단탭·홈·마일리지)
│     └─ admin.css          페이지 관리 전용 (미리보기 프레임·편집 블록·동적 행)
│
└─ js/
   ├─ firebase-config.js    ★ Firebase 연결 설정 (프로젝트 값 붙여넣는 곳)
   ├─ lib/                  ⚙️ 공통 모듈 (화면과 무관)
   │  ├─ firebase.js        SDK 초기화, auth·db·Firestore 함수 내보내기
   │  ├─ account.js         계정↔이메일 변환, 비밀번호 힌트 암호화
   │  ├─ format.js          esc, 날짜/월/페이스 포맷, 카테고리 색
   │  └─ ui.js              $, 모달 열기/닫기, 폼 메시지
   └─ pages/
      ├─ public/
      │  ├─ main.js         소개 페이지 동작 (Firestore 콘텐츠 로드, 빈 섹션 숨김)
      │  └─ site-data.js    소개 페이지 기본값 (문구·현황·가치·정기런 — 저장 내용이 우선)
      ├─ app/               크루 공간 (화면별 1파일)
      │  ├─ main.js         진입점 (설정 확인 → init.js 로드)
      │  ├─ init.js         인증 라우팅·실시간 구독·하단 탭 전환
      │  ├─ views.js        화면(로딩/로그인/대기/본화면) 전환
      │  ├─ state.js        공유 상태 (로그인 세션·데이터·화면 상태)
      │  ├─ auth.js         로그인·가입신청·비밀번호 찾기·내 정보
      │  ├─ home.js         홈 대시보드
      │  ├─ events.js       일정·출석 현황·이달의 기록(마일리지)
      │  ├─ news.js         소식 (공지 + 투표)
      │  └─ members.js      멤버 목록·승인 관리
      └─ admin/
         └─ main.js         페이지 관리 동작 (콘텐츠 저장, 사진 압축 업로드)

firebase.json               Hosting 배포 설정 (no-cache 헤더, SPA rewrite)
firestore.rules             DB 보안 규칙
firestore.indexes.json      Firestore 인덱스 (현재 기본값)
.github/workflows/          master 푸시 → 자동 배포 / PR → 미리보기 배포
PROJECT.md · CHANGE.md      프로젝트 설명서 · 변경 이력
```

- 빌드 도구 없이 브라우저 ES 모듈(`import`)로 동작합니다 — 배포 방식은 그대로.
- 새 기능은 해당 `pages/` 파일에 먼저 넣고, 두 화면 이상에서 쓰게 되면 `lib`/`components` 로 승격하세요.

## 처음 설정 (최초 1회, 약 15분)

이미 운영 중인 프로젝트(`carrotrunningcrew`)에는 필요 없고, **새 Firebase 프로젝트로 옮길 때**만 참고하세요.

1. **Firebase 프로젝트 만들기** — https://console.firebase.google.com → 프로젝트 추가
2. **웹 앱 등록** — 프로젝트 홈에서 웹(`</>`) 아이콘 → 앱 등록 → `firebaseConfig` 값 복사
3. **설정 붙여넣기** — `public/js/firebase-config.js` 의 `window.FIREBASE_CONFIG` 를 복사한 값으로 교체
4. **Authentication 켜기** — 로그인 방법에서 **이메일/비밀번호** 사용 설정
   (화면에서는 "계정"으로 보이지만 내부적으로 이 방식을 사용)
5. **Firestore 만들기** — 위치 `asia-northeast3 (서울)` 권장, 프로덕션 모드
6. **배포** — 터미널(PowerShell)에서:
   ```bash
   npm install -g firebase-tools   # 최초 1회
   firebase login
   cd d:\Development\welcome2crc
   firebase use --add              # 프로젝트 선택
   firebase deploy                 # 규칙 + 사이트 배포
   ```
   > Git Bash 에서 `firebase login` 은 에러가 납니다 (`winpty firebase login` 으로는 가능)
7. **첫 운영진 지정** — 사이트에서 가입 신청 → 콘솔 → Firestore → `members` → 본인 문서의
   `role` 을 `admin` 으로 수정 → 새로고침

## 평소 배포 방법

| 무엇을 바꿨나 | 배포 방법 |
|---|---|
| `public/` 안의 파일 (HTML/CSS/JS) | `git push` 만 하면 GitHub Actions 가 **자동 배포** |
| `firestore.rules` (보안 규칙) | 자동 배포에 **포함되지 않음** → `firebase deploy --only firestore:rules` 직접 실행 |
| 소개 페이지 내용 (문구·기록·사진) | 배포 불필요 — 페이지 관리에서 저장하면 즉시 반영 |

- HTML/JS/CSS 에 `no-cache` 헤더가 설정되어 있어 배포 직후 새로고침만 하면 최신 버전이 보입니다.
  (이전 화면이 보이면 강력 새로고침: PC `Ctrl+F5`, 모바일은 브라우저 캐시 삭제)
- PR을 올리면 미리보기 주소로 자동 배포됩니다 (GitHub Actions).

## 로컬 미리보기

JS 모듈을 사용하므로 파일을 직접 열면 안 되고 간단한 서버가 필요합니다:

```bash
cd d:\Development\welcome2crc
npx serve public          # → http://localhost:3000
# 또는: python -m http.server 8123 (public 폴더에서)
```

## 요금 — 무료 플랜(Spark)으로 충분

- **Firestore**: 저장 1GB, 일 5만 회 읽기 / 2만 회 쓰기 무료 — 수십 명 크루 규모로는 여유
- **Authentication**: 일반 로그인 무제한 무료
- **Hosting**: 저장 10GB, 월 360MB 전송 무료

## 알아두면 좋은 것

- **로그인 계정과 멤버 문서는 별개입니다.** 로그인 계정은 콘솔의 **Authentication → 사용자**에,
  프로필은 **Firestore → `members`** 에 있습니다. members 문서(또는 사이트의 내보내기/기록 삭제)만
  지우면 로그인 계정은 남아서 같은 계정으로 재가입 시 "이미 사용 중인 계정이에요"가 뜹니다.
  **계정 완전 삭제는 Authentication → 사용자 탭**에서 하세요.
  (같은 계정으로 재가입하면 이전 비밀번호 힌트는 자동으로 덮어써집니다)
- "내보내기"한 멤버가 다시 로그인하면 자동으로 '승인 대기' 상태로 돌아옵니다.
- 크루원 본인은 자기 members 문서의 **이름(name)만** 수정할 수 있습니다 (role 변경은 규칙으로 차단).
- 투표는 표가 선택지 번호로 저장되므로, **진행 중인 투표의 선택지 수정은 오타 고치기 정도만** 안전합니다
  (순서를 바꾸거나 줄이면 기존 표가 엉킴 — 수정 모달에도 안내 표시됨).
- 운영진이 소개 페이지 콘텐츠를 저장한 적 없으면: 문구·현황·가치·정기런은 `site-data.js` 기본값이 보이고,
  공식 기록·갤러리·가입 안내·가입 문의 섹션은 아예 숨겨집니다.
